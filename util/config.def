/* CFGSECTION(name, ...) */
/* CFG(name, function, default) */

CFGSECTION(ui,
    CFG(window_width, INT, 1920)
    CFG(window_height, INT, 768)
    CFG(n_patterns, INT, 8)
    CFG(pattern_width, INT, 150)
    CFG(pattern_height, INT, 300)
    CFG(pattern_name_x,INT,25)
    CFG(pattern_name_y,INT,50)
    CFG(intensity_gain_x, FLOAT, 0.01)
    CFG(intensity_gain_y, FLOAT, 0.01)
    CFG(crossfader_x, INT, 900)
    CFG(crossfader_y, INT, 300)
    CFG(crossfader_width, INT, 150)
    CFG(crossfader_height, INT, 300)
    CFG(crossfader_gain_x, FLOAT, 0.01)
    CFG(crossfader_gain_y, FLOAT, 0.01)
    CFG(font,STRING,"ProggyTinySZ.ttf")
    CFG(fontsize, INT, 24)
    CFG(pat_entry_x, INT, 15)
    CFG(pat_entry_y, INT, 60)
    CFG(pat_entry_prompt,STRING,">:")
    CFG(spectrum_x, INT, 100)
    CFG(spectrum_y, INT, 550)
    CFG(spectrum_width, INT, 400)
    CFG(spectrum_height, INT, 200)
    CFG(waveform_x, INT, 500)
    CFG(waveform_y, INT, 550)
    CFG(waveform_width, INT, 400)
    CFG(waveform_height, INT, 200)
    CFG(fps, FLOAT, 75)
    CFG(strip_thickness, FLOAT, 0.05)
    CFG(font_atlas_width,INT,512)
    CFG(font_atlas_height,INT,512)
)

CFGSECTION(deck,
    CFG(n_patterns, INT, 100)
)

CFGSECTION(pattern,
    CFG(master_width, INT, 100)
    CFG(master_height, INT, 100)
    CFG(dir, STRING, "resources/patterns/")
)

CFGSECTION(audio,
    CFG(chunk_size, INT, 256)
    CFG(sample_rate, INT, 48000)
    CFG(fft_length, INT, 2048)
    CFG(spectrum_bins, INT, 256)
    CFG(waveform_length, INT, 512)
)

CFGSECTION(paths,
    CFG(resource_path,STRING,"resources")
    CFG(params_config, STRING, "resources/params.ini")
)
#undef CFGSECTION
#undef CFGSECTION_LIST
#undef CFG
#undef CFG_LIST
